let A = 5;//parseInt(lines[0]);
let B = 6; //parseInt(lines[1]);
let C = -7; //parseInt(lines[2]);
let D = 8; //parseInt(lines[3]);
let diferenca = (A * B - C * D);
console.log("DIFERENCA = " + diferenca);